*.log
*.sqlite-journal
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
node_modules/
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# The zero-config demo database, created by `php artisan migrate`.
/database/*.sqlite
# Ignore it anywhere, not just under database/: a working database that lands
# in a commit carries real rows, and makes tests and diffs non-reproducible.
/data/*.sqlite
*.sqlite
